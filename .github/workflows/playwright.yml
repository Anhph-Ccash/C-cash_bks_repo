name: CI (no Playwright)
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Playwright workflow removed. Kept a minimal CI workflow placeholder that can be
# extended later for linting, unit tests, or other non-Playwright tasks.
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: No-op
        run: echo "Playwright removed; no CI steps configured."

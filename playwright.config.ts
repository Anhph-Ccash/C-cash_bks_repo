// Playwright configuration removed.
// The project no longer uses Playwright tests. This file was retained as a placeholder
// to avoid breaking any tooling that expects it, but it contains no active config.

export default {};
